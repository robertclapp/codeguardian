import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aihrplatform.app',
  appName: 'AI HR Platform',
  webDir: 'dist/public'
};

export default config;
